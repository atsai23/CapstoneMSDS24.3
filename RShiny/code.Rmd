---
title: "Untitled"
author: "Anna Tsai"
date: "`r Sys.Date()`"
output: html_document
---

```{r}
library(shiny)
library(tidy)
```

```{r}
df <- read.csv("mergetemp_test.csv")
```

```{r}
df <- read.csv("mergetemp_test.csv")
  
df <- mutate(df, Date = as.Date(Date, format= "%d.%m.%Y"))
```

